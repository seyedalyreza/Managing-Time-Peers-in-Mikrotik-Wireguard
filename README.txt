#about code
1. The `endTime` script only works if the comment includes just the time; it does not work otherwise.
2. After the `startTime`, you need to manage the peers' time manually.
3. You have to add a schedule for each script.
